# مستندات Renalloc

این پوشه مرجع فنی و عملیاتی پروژه است و عمداً در رابط کاربری نمایش داده نمی‌شود.

- `clinical-matching.md`: قواعد ABO، تابعیت، HLA، Anti-HLA، CREG و Matching اهداکننده جسد
- `registry-workflows.md`: ثبت پرونده، ورود و خروج از لیست انتظار، اولویت و پردازش Celery

این اسناد جایگزین پروتکل مصوب تیم پیوند، نظر آزمایشگاه HLA یا Cross-Match فیزیکی نیستند.
